# 触灵工坊工程说明

## 项目定位

触灵工坊是一个 Android 脚本工具原型工程，目标是提供：

- 面向用户的脚本编辑器
- 中文伪代码 DSL
- `DSL -> AST -> IR -> Lua` 的编译链
- 基于 `AccessibilityService` 的自动化执行能力
- 悬浮窗式的脚本控制入口

当前项目仍处于原型阶段，重点是把脚本编辑、脚本校验、脚本执行链路和悬浮窗控制条打通。

## 当前已实现功能

### 1. 页面结构

当前 App 采用底部导航，包含 3 个主页面：

- `首页`
  - 通过系统文件选择器加载脚本文件
  - 读取当前已选脚本
  - 对脚本做 DSL 编译校验
  - 校验通过后，可直接启用正式执行用悬浮窗

- `脚本编辑器`
  - 顶部显示当前脚本名
  - 右上角提供 `编译 / 调试` 两个等样式按钮
  - 菜单栏采用桌面编辑器风格，包含：`文件 / 插入操作 / 编辑`
  - `插入操作` 已按 `模拟操作 / 逻辑 / 系统操作` 分组成级联菜单
  - 中间是深色代码编辑区，包含行号列
  - 底部是可折叠的编译结果区
  - `调试` 支持直接执行当前编辑内容，不强制先保存到文件

- `设置`
  - 打开无障碍服务设置
  - 打开悬浮窗权限设置
  - 打开通知权限设置

### 2. 脚本编辑与文件存储

脚本主流程已经切到文件系统与系统文档选择器，不再依赖 Room 作为页面脚本来源：

- 用户正式脚本目录：用户授权选择的文件夹
- 识图素材目录：用户授权选择的文件夹下的 `Images`
- 首次启动会引导用户选择脚本工作目录，用户选哪个文件夹就直接使用哪个文件夹
- 首页“加载脚本”和编辑器“打开文件 / 另存为”均通过系统文件选择器工作
- 仍保留应用私有目录下的欢迎脚本种子文件，作为内部兜底脚本来源
- 支持：
  - 新建脚本
  - 保存
  - 另存为
  - 打开系统脚本文件
  - 直接调试当前未保存内容
  - 撤销

### 3. DSL 与编译链

当前 DSL 已实现基础语法解析和编译，支持：

- `点击 x y`
- `长按 x y duration`
- `滑动 x1 y1 x2 y2 duration`
- `键盘输入 "文本"`
- `等待 ms`
- `设 结果 = 识图 "图片文件名" confidence`
- `启动应用 "包名"`
- `记录 "文本"`
- `设 变量 = 表达式`
- `循环 N 次 ... 结束循环`
- `无限循环 ... 结束循环`
- `如果 条件 ... 否则 ... 结束如果`
- `返回`
- `主页`
- `停止运行`

编译链为：

- `ScriptParser`
- `AST`
- `IR`
- `LuaGenerator`
- `LuaJ` 执行

编辑器侧已经补充若干程序员友好交互：

- 插入 `循环 / 如果` 时，会自动生成结构骨架
- 结构骨架中会预留更深一层缩进的空行
- 插入后光标会自动落到首个可编辑代码块
- 普通回车会继承当前缩进
- 在 `循环 / 如果 / 否则` 这类结构行后回车，会自动再缩进一层
- 在 `结束循环 / 结束如果` 前的缩进区按退格，会回退一整层缩进
- 插入操作会按当前光标所在行自动对齐缩进，并插到当前光标位置而不是文末

### 4. 自动化执行

当前自动化能力基于：

- `AccessibilityService`
- `Foreground Service`
- `LuaJ Runtime`
- Host API 桥接

Lua 侧会调用受控宿主 API，例如：

- `touch.click(...)`
- `touch.longPress(...)`
- `touch.swipe(...)`
- `keyboard.input(...)`
- `device.sleep(...)`
- `device.back()`
- `device.home()`
- `app.launch(...)`
- `log.info(...)`
- `image.find(...)`
- `runtime.stop()`

识图表达式返回结果对象，DSL 侧可通过 `结果.找到`、`结果.x`、`结果.y`、`结果.置信度` 读取是否找到、坐标和实际匹配分数。

编辑器中的“调试”链路与首页正式执行链路已分离：

- 首页悬浮窗默认执行当前已选中的正式脚本文件
- 编辑器“调试”会把当前内存中的脚本文本作为临时调试稿直接交给执行链
- 调试稿不会强制落盘，也不会覆盖首页当前选中的正式脚本

### 5. 悬浮窗

当前已实现两类悬浮能力：

- 执行控制悬浮窗
  - 样式为圆角横条
  - 已包含按钮：
    - 启动/停止
    - 暂停/继续
    - 日志
    - 退出悬浮窗
  - 状态与日志已迁移到悬浮窗逻辑中维护

- `抓抓` 取点悬浮窗
  - 顶部小条包含：`抓取 / 退出`
  - 抓取时会出现灰色挡板
  - 点击屏幕后会显示坐标提示
  - 落点位置会显示“十字线 + 空心圆”准星
  - `点击 / 长按 / 滑动` 的插入弹窗左下角都已接入 `抓抓` 入口
  - `滑动` 支持连续抓取起点与终点并自动回填

注意：

- 悬浮窗显示依赖系统悬浮窗权限
- 脚本动作执行依赖无障碍服务权限
- 当前“停止”已改成尽量立即生效：
  - 暂停时按停止不会再卡住
  - 运行时按停止不会再继续把后续动作跑完
  - 对 `等待` 动作已做分段轮询中断
  - 已经发给系统的单个超长手势，系统层面未必能硬中断

### 6. 数据与状态

当前工程同时包含两类数据存储：

- `FileScriptRepository`
  - 负责脚本文件、系统目录读写、工作目录创建
  - 是当前页面主流程使用的脚本来源

- `Room + ScriptRepository`
  - 当前仍保留
  - 主要用于运行记录等结构化数据
  - 后续可继续精简或统一

设置项使用 `DataStore`，当前主要保存：

- 编辑器字体缩放
- 默认延迟
- 当前选中的脚本路径与脚本名
- 用户授权后的脚本工作目录 Uri

另外，自动化调试还新增了一层运行期状态：

- 编辑器临时调试稿会保存在内存态 `DebugScriptDraftStore`
- 仅供编辑器右上角“调试”按钮调用
- 首页加载脚本或启用正式悬浮窗时，会清空该临时调试稿

## 当前工程结构

### 核心入口

- `app/src/main/java/com/lulucloud/touchscript/MainActivity.kt`
- `app/src/main/java/com/lulucloud/touchscript/TouchWorkshopApplication.kt`
- `app/src/main/java/com/lulucloud/touchscript/app/AppContainer.kt`

### 主要模块

- `feature/home`
  - 首页逻辑

- `feature/editor`
  - 脚本编辑器

- `feature/settings`
  - 设置页

- `core/script`
  - DSL 解析、AST、IR、Lua 生成

- `core/runtime`
  - Lua 运行时与宿主桥接

- `core/automation`
  - 无障碍执行、执行状态、悬浮窗、前台服务

- `data/repository`
  - 文件脚本仓库、设置仓库、运行记录仓库

## 已验证内容

目前工程已验证过以下构建链路：

- `.\gradlew.bat assembleDebug`
- `.\gradlew.bat testDebugUnitTest`
- `.\gradlew.bat installDebug`

并且已经在真机上解决过一次 Compose 启动闪退问题。

## 当前已知限制

- 悬浮窗交互虽然已落代码，但仍属于原型阶段，细节体验还需要继续打磨
- 编辑器级联菜单目前仍基于 Compose `DropdownMenu` 近似实现，视觉和定位还可以继续细调
- 首页、导航、弹窗等中文文案在部分文件中经历过多轮改动，后续建议统一做一次文案和编码清理
- Room 仍保留了一部分原型阶段数据结构，后续可以决定是否继续保留
- 还没有接入 OCR、Root、Shizuku、手势录制等更高级能力

## 后续协作建议

如果后续继续开发，建议优先按下面顺序推进：

1. 清理页面与仓库中的中文乱码/旧文案
2. 继续细化编辑器菜单、输入体验和代码结构编辑能力
3. 把悬浮窗、调试稿、正式脚本三条执行链路完整联调一遍
4. 再评估是否继续精简 Room 与旧原型数据结构
5. 最后再考虑 OCR、手势录制等更高级能力

## 说明

本文件用于帮助后续协作者快速理解当前工程状态，不代表最终产品规格。
